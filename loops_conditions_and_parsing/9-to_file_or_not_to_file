#!/usr/bin/env bash
#utilisation
n=school
if [[ -e "$n" ]];
then 
	echo "school file exists"
elif  [[ -s "$n" ]];
	then
	echo "school file is emty"
elif [[ -f "$n" ]];
	then
		echo "school is a regular file"

		echo "  "
fi
